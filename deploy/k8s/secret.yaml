# DO NOT commit real secrets. Use SealedSecret / ExternalSecret / SOPS.
# This is the shape you want — replace values with your own.
apiVersion: v1
kind: Secret
metadata:
  name: poolsmith-userlist
  namespace: poolsmith
type: Opaque
stringData:
  userlist.txt: |
    "app"             "CHANGE_ME"
    "postgres"        "CHANGE_ME"
    "poolsmith_admin" "CHANGE_ME"
