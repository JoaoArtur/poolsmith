apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: poolsmith

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/part-of: poolsmith
  app.kubernetes.io/managed-by: kustomize
